# Copy to .env and fill locally. NEVER commit .env. NEVER hardcode these in source.
# Use a throwaway hackathon wallet — demo funds only.

# Contracts (forge script) + ops scripts
PRIVATE_KEY=
RPC_URL=
BACKEND_SIGNER=
CHECKER_ADDRESS=

# Uniswap Trading API helper (scripts/swap.ts)
UNISWAP_API_KEY=
TRADE_CONFIRM=
