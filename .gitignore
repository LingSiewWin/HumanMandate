# ── secrets: never committed, never hardcoded ──
.env
.env.*
!.env.example
*.key
*.pem

# ── private docs: only README.md is public ──
*.md
!/README.md
# FEEDBACK.md is a mandatory public qualification artifact for the Uniswap
# Foundation prize (the submission must link to it), so it ships deliberately.
!/FEEDBACK.md
docs/
CLAUDE.md
.claude/

# ── deps / build ──
node_modules/
out/
cache/
broadcast/
dist/
.next/
.DS_Store

# foundry deps (reinstall: forge install uniswap/v4-periphery --no-git)
contracts/lib/

# MCP config contains API keys
.mcp.json
nullifiers.json
.vercel/

.keys/

# local browser / MCP artifacts
.playwright-mcp/

nullifiers.json
